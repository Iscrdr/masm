基本运算：+ , - , * , / , mod, ++, --
比较计算：>, < ,>= , <= ，!=, ==
位运算：<< >> >>>   <<<
逻辑运算：& 与 ,| 或 ,^ 异或 ,~ 取反 && ||
赋值运算：=，+=，-=，*=，/=，%=，<<= , =>>, &=,^=,|=

其它：三元运算 ？：

计算规则：顺序：从左至右，由内到外
() [] ++ --

+ - ! ~ ++ - - 

* / % 

+ - 
位移：<< >> 
关系：<<=  >>= 
== != 


&&,||


循环结构：
for(){}
while(){}
do{}while()

break,continue

条件分支
if(){}
else if() {} else{}


switch(expression){
    case value :
       //语句
       break; //可选
    case value :
       //语句
       break; //可选
    //你可以有任意数量的case语句
    default : //可选
       //语句
}

try catch finally

throw / throws

对象 class
抽象 abstract
继承 extends 
接口 interface
实习 impl


函数：Behavior 


重写(Override)与重载(Overload)

@Override

多态@Override（parentBy=Parent.class）


访问权限：

public
protected
default（包权限，不写关键字）
private

static,const,super,this,enum

AOP
Annotation

lambda:
(参数) -> { 执行语句 }

泛型

泛型 = 范围
多态 = 范围内的转换



向上 = 自动 
向下 = 运行校验
兄弟 = 禁止
集合 = 唯一子类型（安全）
 